import React from 'react';
import ReactDOM from 'react-dom/client';
import Primero from './terminal';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
   <Primero />
  </React.StrictMode>
);


